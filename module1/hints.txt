каждый раз делаем шаг по массиву в 2 р больше предыдущего, проверяем вхождение искомого элемента в диапазон [old, current] -- на нем делаем бинарный поиск позиции элемента

правило единоличного владения

не проеряем выходы за границы структур

cpp unit

использование разных областей видимости в рамках
одной функции

std::stringstream - удобный интерфейс ввода/вывда через строку

test input.str()/output.str()
for (const char c : str) {
    std::cout << std::hex << (int)c << std::endl;
}

std::function

bash: sort
bash: unique

реорганизация кучи происходит за logK (k - число элементов кучи)
т.к. всего N эелментов, то итоговая сложность N * logK